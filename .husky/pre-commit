#!/usr/bin/env bash
set -e

# Tests: en macOS forzar Node arm64 (evita conflicto sqlite3 arm64 vs x86_64)
echo "🧪 Running validation tests..."
case "$(uname -s)" in
  Darwin) arch -arm64 npm test ;;
  *)      npm test ;;
esac
